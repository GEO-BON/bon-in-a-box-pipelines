script: assertInt.py
name: Assert int
description: "Test python script that fails if it doesn't receive an integer."
author:
  - name: Jean-Michel Lord
inputs:
  input:
    label: Some int
    description: Value to be validated
    type: int
    example: 42
outputs:
  the_same:
    label: Same value
    description: The same integer as in the inputs. But this crashes if not an integer.
    type: int
    example: false
