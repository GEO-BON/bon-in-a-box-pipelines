script: checkFile.py
inputs:
  file:
    label: Some file
    description: The file that we will check for presence
    type: application/octet-stream
    example: some_file.txt
outputs:
  presence:
    label: The same array as in the inputs. But this crashes if no an array
    description: bla bla
    type: boolean
    example: [3,4,5]
description: "Sample python script fail if it doesn't receive an array."