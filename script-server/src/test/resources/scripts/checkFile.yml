script: checkFile.py
name: Check file
description: "Sample python script fails if given file does not exist"
author:
  - name: Jean-Michel Lord
inputs:
  file:
    label: Some file
    description: The file that we will check for presence
    type: application/octet-stream
    example: some_file.txt
outputs:
  presence:
    label: The same array as in the inputs. But this crashes if no an array
    description: bla bla
    type: boolean
    example: [3,4,5]