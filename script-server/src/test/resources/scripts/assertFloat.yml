script: assertFloat.py
name: Assert float
description: "Test python script that fails if it doesn't receive a float."
author:
  - name: Jean-Michel Lord
inputs:
  input:
    label: Some float
    description: The value to check.
    type: float
    example: 12.34
outputs:
  the_same:
    label: Same value
    description: The same float as in the inputs. But this crashes if not an float.
    type: float
    example: 12.34
