script: calculateOHI.R
name: Calculate OHI
description: Calculates the OHI based on ...
author:
  - name: Laetitia Tremblay
    email: laetitia.tremblay@mcgill.ca
    identifier: http://www.linkedin.com/in/laetitia-tremblay-b0619b273

inputs:
  EEZ:
    description: EEZ for the country of interest
    label: EEZ
    type: application/geopackage+sqlite3
    example:
  sp_status:
    description: List of species and their threat status
    label: Species status
    type: text/csv
    example:
  country_region_bbox:
    label: Bounding box
    description: Use the bounding box chooser to select a region or create a custom bounding box (region selections will be ignored for EEZs since they are national).
    type: bboxCRS
    example:
  range_maps:
    label: expert range map from IUCN
    description: Polygon with expected area for the species.
    type: application/geopackage+sqlite3[]

outputs:
  ohi_results:
    description: OHI results
    label: OHI
    type: text/csv
    example:
conda:
  channels:
    - conda-forge
    - r
  dependencies:
    - r-rjson
    - r-tidyverse
    - r-sf
    - r-rredlist
    - r-here

