script: loadGbif.R
inputs:
  species:
    label:
    description: Occurrence data, some description here
    type: text
    example: "Panthera onca"
  limit:
    description: Occurrence data, some description here
    type: int
    example: 5000
outputs:
  n.observations:
    label: Number of observations
    description:
    type: int
    example: 8
  species:
    label: species
    description:
    type: text
    example: "Panthera onca"
  warning:
     label: warning
     description:
     type: text
     example: "Some warning"
  data:
     label: data
     description: table with observations from GBIF
     type: text/tab-separated-values
     example: 
description: "This script retrieves observations from GBIF database."
external_link: https://github.com/GEO-BON/biab-2.0
