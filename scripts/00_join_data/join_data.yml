script: join_data.R
description: "This script converts the study area layer (shapefile or bounding box) into a spatial grid (well-known text (WKT) representation)."
external_link: https://github.com/GEO-BON/biab-2.0
inputs:
  records_data:
    label: records_data
    description: records_data
    type: text/csv
    example:  "records_data.csv"
  col_join:
    label: col_join
    description: col_join
    type: text
    example: "site"
outputs:
  filter_data:
    label: filter_data
    description: filter_data
    type: text/csv