script: SDM_test_v1.R
inputs:
outputs:
  prediction:
    label: Prediction
    description: Sample prediction
    type: geotiff
    example: 
  uncertainty:
    label: Uncertainty
    description: Sample uncertainty 
    type: geotiff
    example: 
description: "Running an arbitrary SDM, don't look too much at the results."